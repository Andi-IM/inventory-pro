# Workflow Engine
CAMUNDA_REST_URL=http://localhost:8080/engine-rest

# Neon Auth Upstream
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=

# Server & Client Feature Flags
NEXT_PUBLIC_ALLOW_SIGN_UP=false

# Postgres Connection
DATABASE_URL=

# Redis Cache (Optional)
# Used for caching feature flags. Falls back to Postgres if omitted.
# Example: redis://localhost:6379
REDIS_URL=